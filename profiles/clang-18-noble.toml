# Clang 18 on Noble — standard profile, no extra flags.
# Use this to measure the baseline drop-in replacement gap between GCC and Clang.

[compiler]
type = "clang"
version = "18"

[target]
series = "noble"
