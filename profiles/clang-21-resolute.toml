# Clang 21 on Resolute — standard profile, no extra flags.
# Use this to measure the baseline drop-in replacement gap between GCC and Clang.

[compiler]
type = "clang"
version = "21"

[target]
series = "resolute"
